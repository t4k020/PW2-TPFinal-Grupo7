<style>
    body {
        min-height: 100vh;
        background: linear-gradient(
                180deg,
                #e5e7e8 0%,
                #f8f9fa 100%
        );
    }

    header {
        background: linear-gradient(rgba(13, 110, 253, 0.75),
        rgba(13, 110, 253, 0.25)),
        url('/public/img/banner.jpg');

        background-size: cover;
        background-position: center;

        min-height: 280px;

        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;

        text-align: center;

        color: white;

        border-bottom: 4px solid #ffc107;
        box-shadow: 0 5px 20px rgba(0, 0, 0, .3);
    }

    header h1 {
        font-size: clamp(4rem, 5vw, 5rem);
        font-weight: 900;
        color: white;

        text-shadow: 0 2px 0 #041a42,
        0 4px 0 #021945,
        0 6px 12px rgba(0, 0, 0, .4);

        letter-spacing: -2px;
    }

    header p {
        font-size: 1.2rem;
        opacity: .95;
    }

    main.container {
        margin-top: -60px;
        position: relative;
        z-index: 10;
    }

    #mapa {
        height: 400px;
        width: 100%;
        margin-bottom: 15px;
        border-radius: 8px;
        border: 1px solid #ccc;
    }

</style>

<header>
    {{#usuario}}
        <div class="position-absolute top-0 end-0 p-3">
            {{#esAdmin}}
                <a href="/PanelAdmin" class="btn btn-warning shadow fw-bold me-2">
                    ⚙️ Panel Admin
                </a>
            {{/esAdmin}}

            {{#esEditor}}
                <a href="/PanelEditor/mostrar" class="btn btn-dark shadow fw-bold me-2 text-white border-light">
                    📝 Panel Editor
                </a>
            {{/esEditor}}

            <a href="/login/logout" class="btn btn-danger shadow">
                🔓 Cerrar sesión
            </a>
        </div>
    {{/usuario}}

    <a class="text-decoration-none" href="/lobby"><h1>Preguntados</h1></a>
    <p>"AQUÍ SE FORJAN LAS LEYENDAS DEL CONOCIMIENTO"</p>
</header>